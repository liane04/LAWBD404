Remove-Migration
Add-Migration InitialCreate
Update-Database -Context ApplicationDbContext 



Login 

admin@email.com, vendedor@email.com, comprador@email.com

pass.
123



*Se der erro no login :
# Aplicar migração existente primeiro
  dotnet ef database update --context ApplicationDbContext

  # Criar nova migração para ImagemPerfil
  dotnet ef migrations add AddImagemPerfilColumn --context ApplicationDbContext

  # Aplicar a nova migração
  dotnet ef database update --context ApplicationDbContext